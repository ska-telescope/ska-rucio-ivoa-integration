image:
  registry: registry.gitlab.com
  repository: ska-telescope/src/ska-rucio-ivoa-integration/postgres-metadata
  tag: bitnami-dachs
  pullPolicy: Always

auth:
  username: metadata
  password: secret
  database: metadata
  existingSecret: null

primary:
  persistence:
    enabled: true
    storageClass: csi-cinder-sc-retain
    size: 250Gi
