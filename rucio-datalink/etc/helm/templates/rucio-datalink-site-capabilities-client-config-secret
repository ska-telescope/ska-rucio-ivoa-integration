apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-site-capabilities-client-config
  namespace: {{ .Values.namespace }}
data:
  site-capabilities-client-cfg-secret: "{{ .Values.siteCapabilitiesClientSecret}}"
