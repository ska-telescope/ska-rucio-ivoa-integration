apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-data-management-client-config
  namespace: {{ .Values.namespace }}
data:
  data-management-client-cfg-secret: "{{ .Values.dataManagementClientSecret}}"
